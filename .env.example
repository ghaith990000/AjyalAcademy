# Copy to .env.local. Hosted: Project URL + publishable key (sb_publishable_…) from the Supabase dashboard → Settings → API.
# Local: the values printed by `npx supabase start`.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
