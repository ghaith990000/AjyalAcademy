# Decision log

ADR-style. Newest at the bottom. Add an entry whenever you make a non-obvious choice or change a rule. Status: **Accepted** unless noted.

## Confirmed by the product owner

| ID    | Decision                                                                                                                                                               |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-001 | **Backend = Supabase** (Postgres + Auth + RLS + Realtime + an Edge Function). Chosen over Firebase/custom API for SQL reports, RLS roles and speed.                    |
| D-002 | **Pricing model:** one shared subscription per group, billed monthly; covers 1–4 players; plan chosen by player count (Solo 20 / Duo 35 / Trio 50 / Quad 60 BD).       |
| D-003 | **Discounts:** admin-defined reusable codes (percent or fixed BD, optional expiry/max uses) **and** a manual one-off discount per subscription with a required reason. |

## Assumptions made by Claude (change freely — tell Claude)

| ID    | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Why / impact                                                                                                                                                                                                                                                  |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D-010 | **Money stored as integer fils** (1 BD = 1000 fils).                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | BHD has 3 decimals; avoids float errors in sums and reports.                                                                                                                                                                                                  |
| D-011 | **One coach per player** (`players.coach_id`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Matches "distribution of players to coaches". Many-to-many would complicate rosters/permissions.                                                                                                                                                              |
| D-012 | **T-shirt and transport fees are per player** inside a subscription; T-shirt only for a player's first-ever subscription (editable by admin).                                                                                                                                                                                                                                                                                                                                                                                   | The request says "for first time joining" and "if the player wants transportation".                                                                                                                                                                           |
| D-013 | **Discount applies to the full subtotal** (plan + T-shirt + transport), capped at the subtotal.                                                                                                                                                                                                                                                                                                                                                                                                                                 | Simplest reading; one function to change if the owner wants plan-only.                                                                                                                                                                                        |
| D-014 | **"Collected" = payments** (by `paid_at`), not subscription totals. Subscriptions default to "paid in full" on creation; partial/later payments supported.                                                                                                                                                                                                                                                                                                                                                                      | The request says "fees collected". Enables balances and unpaid tracking.                                                                                                                                                                                      |
| D-015 | **Soft delete** for players and subscriptions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Preserve financial/attendance history; log who removed.                                                                                                                                                                                                       |
| D-016 | **Coach permissions:** add/edit/remove own players; create subscriptions & payments for own players; schedule own sessions and take attendance; see own activity only. Admin-only: coaches, assignment, plans/settings, discounts, expenses, reports, full feed.                                                                                                                                                                                                                                                                | Reflects "every coach manages a list of players" + "admin distributes players".                                                                                                                                                                               |
| D-017 | **Profit margin = (collected − expenses) ÷ collected**, per month/year.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Standard definition; shows "—" when collected = 0.                                                                                                                                                                                                            |
| D-018 | **Arabic default (RTL)**, English toggle; **Latin digits** in both; Gregorian dates.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Bahrain convention; consistent with CPR/phone input.                                                                                                                                                                                                          |
| D-019 | **Activity log written server-side** (triggers/RPCs), never from the client; stores a snapshot summary.                                                                                                                                                                                                                                                                                                                                                                                                                         | Tamper-proof audit; survives deletes.                                                                                                                                                                                                                         |
| D-020 | **Composite writes via Postgres RPCs** (create_subscription, save_attendance…), and the subscription total is recomputed server-side.                                                                                                                                                                                                                                                                                                                                                                                           | Atomicity + don't trust client totals. Formula exists in TS (preview) and SQL (authoritative).                                                                                                                                                                |
| D-021 | **No self-signup.** Admin creates coach accounts through a `create-coach` Edge Function (service role). The first admin is seeded.                                                                                                                                                                                                                                                                                                                                                                                              | Closed internal system.                                                                                                                                                                                                                                       |
| D-022 | **Theme derived from the logo** (blue `#1860C8`, pink `#E02068`, white shield, navy for depth) because no public brand guide exists. Fonts Cairo + Poppins.                                                                                                                                                                                                                                                                                                                                                                     | Owner asked to "match their theme"; see [06-design-system.md](06-design-system.md).                                                                                                                                                                           |
| D-023 | **Attendance status = present / absent** only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Keep it simple and fast on mobile; extend (late/excused) later if asked.                                                                                                                                                                                      |
| D-024 | **Session roster = the session coach's active players**; players lacking an active subscription get a warning badge but can be marked.                                                                                                                                                                                                                                                                                                                                                                                          | Predictable and simple; no per-session enrollment.                                                                                                                                                                                                            |
| D-025 | **Tooling:** Vite 8 + React 19 + TS 6, Tailwind v4, Vitest 5, ESLint 10 (flat config), single `tsconfig.json`, npm.                                                                                                                                                                                                                                                                                                                                                                                                             | Latest stable at scaffold time (Sep 2026).                                                                                                                                                                                                                    |
| D-026 | **`Select` is a native `<select>`**, not Radix Select.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Best phone UX (OS picker), RTL-safe, zero JS; Radix Select is heavier and awkward on mobile.                                                                                                                                                                  |
| D-027 | **Phone tab labels can differ from page titles** (`tabLabel`): English "Billing" for Subscriptions.                                                                                                                                                                                                                                                                                                                                                                                                                             | 13px labels must fit ~78px; Arabic label fits as-is.                                                                                                                                                                                                          |
| D-028 | **Typed i18n keys from the English JSON**, namespaces registered in `src/lib/i18n-resources.ts`; ar/en parity and the physical-direction-class guard are Vitest tests (`src/test/conventions.test.ts`) rather than an ESLint rule.                                                                                                                                                                                                                                                                                              | Catches missing keys at compile time; the guard runs with `npm test` so it can't be skipped.                                                                                                                                                                  |
| D-029 | **Shield is an SVG component; Radix is consumed through the unified `radix-ui` package**; `class-variance-authority` for component variants.                                                                                                                                                                                                                                                                                                                                                                                    | One dependency, tree-shakeable; replaces the planned CSS clip-path shield (a clip-path can't draw the curved outline).                                                                                                                                        |
| D-030 | **The hosted Supabase project is the development backend** (no Docker needed). Migrations are applied to it with the Supabase MCP (`apply_migration`); the files in `supabase/migrations/` stay the source of truth. Local `npx supabase start` still works (`config.toml`, `seed.sql`).                                                                                                                                                                                                                                        | The owner connected a hosted project. MCP records each migration under its _apply_ timestamp, so the hosted history names differ from the file timestamps; content is identical. If a CLI-driven flow is adopted later, run `supabase migration repair` once. |
| D-031 | **Database tests are pgTAP** files in `supabase/tests/database/`, each inside a transaction that is rolled back. Run with `npx supabase test db` (local) or through the MCP `execute_sql` against the hosted project. An in-process PGlite harness was tried and **removed** at the owner's request.                                                                                                                                                                                                                            | Tests the real Supabase (real `auth`, roles, grants), and never leaves data behind. 131 assertions cover isolation, guards, constraints, activity logging and a privilege audit.                                                                              |
| D-032 | **Later-phase RPCs are not stubbed in Phase 2.** Phase 2 ships `remove_player` only; `create_subscription`, `record_payment`, `save_attendance`, reports, … arrive with the phase (and tests) that use them.                                                                                                                                                                                                                                                                                                                    | A stub that nothing calls and nothing tests is dead code; signatures in `04-data-model.md` remain the contract.                                                                                                                                               |
| D-033 | **Player removal is the `remove_player(id)` RPC** (SECURITY DEFINER, checks ownership), not a plain `UPDATE deleted_at`. Restoring is admin-only.                                                                                                                                                                                                                                                                                                                                                                               | Postgres re-checks the SELECT policy on the updated row, and removed players are deliberately invisible to coaches, so a plain UPDATE fails for them.                                                                                                         |
| D-034 | **A coach can read only their own profile** (the matrix said "self + names of all coaches").                                                                                                                                                                                                                                                                                                                                                                                                                                    | Stricter and simpler: coaches never see other coaches' data. Display names come from `activity_log.summary` snapshots or RPC joins.                                                                                                                           |
| D-035 | **Helper is `current_user_role()`**, not `current_role()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `current_role` is a reserved SQL keyword.                                                                                                                                                                                                                     |
| D-036 | **Deny by default:** `anon`, `authenticated` and `PUBLIC` get no privileges on new tables/functions; each migration grants back exactly what is needed. The privilege-audit tests fail if this regresses. Supabase's advisor flags the helper functions as "executable by signed-in users" — intentional, RLS policies call them as the invoking role.                                                                                                                                                                          | Defence in depth beyond RLS.                                                                                                                                                                                                                                  |
| D-037 | **Auth behaviour:** users whose profile is missing or inactive are signed out (at login and mid-session); the profile's `preferred_language` is applied once after sign-in and later language toggles are saved back; the TanStack Query cache is cleared on sign-out so data never leaks to the next user of the device.                                                                                                                                                                                                       | Closed internal system on shared phones.                                                                                                                                                                                                                      |
| D-038 | **`coach.created` is logged by the Edge Function** with the service role, not by a trigger.                                                                                                                                                                                                                                                                                                                                                                                                                                     | A trigger on `profiles` would see `auth.uid()` = null and lose the acting admin.                                                                                                                                                                              |
| D-039 | **`VITE_SUPABASE_ANON_KEY` holds the project's publishable key** (`sb_publishable_…`); the legacy anon JWT also works.                                                                                                                                                                                                                                                                                                                                                                                                          | Supabase's recommended key type; same public-by-design role.                                                                                                                                                                                                  |
| D-040 | **No subscription-status badge on the players list in Phase 3.** The column is added in Phase 4 when there is data behind it.                                                                                                                                                                                                                                                                                                                                                                                                   | A "no subscription" badge on every row would be misleading; the phase file only asked for a placeholder.                                                                                                                                                      |
| D-041 | **Players list = server-side search (name / CPR / phone), coach and "has condition" filters, sort by name or newest, 20 per page with a "Show more" button.** Offset paging with `id` as tie-breaker; the search text is stripped of characters that mean something in a PostgREST `or(...)` filter.                                                                                                                                                                                                                            | An academy has hundreds of players, not millions; simple and predictable beats infinite scroll.                                                                                                                                                               |
| D-042 | **A player's coach changes only through the dedicated actions** — the single "Coach assignment" card on the detail page and bulk assign in the list (`assign_players`, admin only, target must be an active coach, unassign allowed, unchanged rows are skipped and not logged). The edit form never sends `coach_id`.                                                                                                                                                                                                          | One audited path; a coach can never reassign by editing.                                                                                                                                                                                                      |
| D-043 | **Duplicate CPR:** the form links to the existing player only if the caller may see it (RLS); otherwise it says the CPR is registered with another coach, without naming anyone.                                                                                                                                                                                                                                                                                                                                                | Blocks the duplicate without leaking another coach's players.                                                                                                                                                                                                 |
| D-044 | **No plural-suffixed i18n keys.** Arabic has six plural forms and the ar/en key-parity test compares raw keys, so counts are plain interpolation (`{{n}}`) in sentences that read correctly for any number.                                                                                                                                                                                                                                                                                                                     | Keeps both languages in lock-step without per-language key sets.                                                                                                                                                                                              |
| D-045 | **Players pages are shared components** mounted under `/admin/players` and `/coach/players`; every link is built from `usePlayersBasePath()` so a coach never lands in the admin area.                                                                                                                                                                                                                                                                                                                                          | Matches the architecture doc; RLS scopes the data.                                                                                                                                                                                                            |
| D-046 | **Phase 4 built on the placeholder answers to Q-001…Q-004:** T-shirt 5.000 BD (once, on a player's first subscription), transport 10.000 BD per player per subscription, coaches may create subscriptions and record payments for their own players, discounts apply to the whole subtotal. All amounts live in **Settings** (admin), so confirming or changing them is a data edit, not a code change (except the discount base, which is in `calc_subscription_total` + `pricing.ts`).                                        | The owner has not answered yet; nothing is hard-coded, and prices are snapshotted so later changes never rewrite history.                                                                                                                                     |
| D-047 | **"Today" is the academy's date (`Asia/Bahrain`):** SQL uses `today_bh()` (the database runs in UTC, 3 hours behind) for discount validity, payment dates and subscription status; the browser uses its local date.                                                                                                                                                                                                                                                                                                             | Between 00:00 and 03:00 in Bahrain the UTC date is still yesterday.                                                                                                                                                                                           |
| D-048 | **`create_subscription` takes the players as JSON** `[{player_id, transport?, tshirt?}]` and recomputes everything. The `tshirt` override is honoured for admins only; a coach's is ignored. The TypeScript total is only a live preview.                                                                                                                                                                                                                                                                                       | Never trust client totals (D-020); one atomic call.                                                                                                                                                                                                           |
| D-049 | **Read models are `security_invoker` views** (`subscription_overview`, `player_subscription_status`) so each caller's RLS applies. A coach sees a mixed subscription with only their own players' names plus "+N with another coach".                                                                                                                                                                                                                                                                                           | Status, paid, balance and player names are computed once in SQL; no data leaks to other coaches.                                                                                                                                                              |
| D-050 | **Coaches may give manual discounts** (a reason is required and it is logged).                                                                                                                                                                                                                                                                                                                                                                                                                                                  | The rules do not restrict it; see Q-009.                                                                                                                                                                                                                      |
| D-051 | **Concurrency:** `create_subscription` locks the selected players' rows (and the discount row) before checking overlap, the first-time T-shirt fee and `max_uses`; `record_payment` / `cancel_subscription` lock the subscription row.                                                                                                                                                                                                                                                                                          | Two people acting at once cannot double-book a player, double-charge the T-shirt or overspend a code.                                                                                                                                                         |
| D-052 | **The wizard's discount-code check is a preview** against the active discounts the user may read; the server is authoritative (including `max_uses`, which a coach cannot count). Codes are matched case-insensitively and trimmed.                                                                                                                                                                                                                                                                                             | Coaches cannot see other coaches' subscriptions, so usage counts are only known server-side.                                                                                                                                                                  |
| D-053 | **A cancelled subscription frees its dates and its use of a discount code; its payments are kept** (no refunds in scope). The first-time T-shirt rule counts cancelled subscriptions (a cancelled first subscription does not re-trigger the fee).                                                                                                                                                                                                                                                                              | Per docs/05-business-rules.md.                                                                                                                                                                                                                                |
| D-054 | **Saving Settings runs the plan-price and fee updates in parallel, not in one transaction.**                                                                                                                                                                                                                                                                                                                                                                                                                                    | Five tiny admin-only updates; a failure is reported and can be retried.                                                                                                                                                                                       |
| D-055 | **Sessions use plain table access (RLS) plus a guard trigger:** the session's coach must be an **active coach**, `created_by` is always the caller, a session cannot be created already cancelled, **cancelling is final** (server-stamped time; a cancelled session is read-only), and a session that already has attendance cannot change coach. A weekly series is one multi-row `insert` (all-or-nothing), not an RPC.                                                                                                      | Rules RLS cannot express; keeps a session's attendance tied to the roster it was taken for.                                                                                                                                                                   |
| D-056 | **The attendance screen starts everyone "absent" and saving sends the whole roster**; `save_attendance` upserts, leaves unchanged rows' `marked_by`/`marked_at` alone and logs one `attendance.saved`. A first save with nobody present is allowed. Marks of players who have since left the roster stay in the database but are not shown or re-sent.                                                                                                                                                                          | Only the players who came need a tap; "nobody came" is a real result; the audit trail shows who changed a mark.                                                                                                                                               |
| D-057 | **The roster's "no active subscription" warning is judged on the session's own date** (`subscription_players` → non-cancelled subscriptions covering it), not through `player_subscription_status`, which describes "today". If the lookup fails, no warnings are shown.                                                                                                                                                                                                                                                        | A session dated last week or next month must be judged against that date.                                                                                                                                                                                     |
| D-058 | **Sessions list:** the default filter "Today & upcoming" shows everything not cancelled from today on, whatever the time; "Done" = not cancelled and over; a row's badge says upcoming until its end time passes. Filters use the browser's clock (as D-047).                                                                                                                                                                                                                                                                   | A coach opens the app right after training to mark attendance — the session that just ended must still be on the default view (phase file: "default today + upcoming").                                                                                       |
| D-059 | **A player's attendance history and rate come from the `player_attendance` view** (`security_invoker`): cancelled sessions are left out; rate = present ÷ counted marks, whole percent half up, "—" when none. Because RLS applies, a coach's numbers only include sessions they run (after a reassignment the new coach sees only their own sessions' marks).                                                                                                                                                                  | Cancelled sessions did not happen; RLS stays the isolation boundary.                                                                                                                                                                                          |
| D-060 | **Time ranges render through `formatTimeRange` inside a plain `<bdi>`, never `dir="ltr"`.** Dates and plain numbers keep `<bdi dir="ltr">`.                                                                                                                                                                                                                                                                                                                                                                                     | Arabic `ص`/`م` are right-to-left characters: a forced LTR box scrambles the range (`4:00 م 5:30 – م`); auto direction keeps English left-to-right and reads correctly in Arabic.                                                                              |
| D-061 | **"Repeat weekly" creates 2–26 sessions, the first included; an overlap with the same coach's sessions is a warning, not a block.**                                                                                                                                                                                                                                                                                                                                                                                             | Matches the phase file; a coach may deliberately double-book (e.g. two age groups), and a hard block would need an override path.                                                                                                                             |
| D-062 | **Attendance can only be taken from the session's day on:** `save_attendance` refuses a cancelled session and one dated after today's academy date (`ajyal:session_in_future`); a session later today can be marked, whatever its start time. The screens mirror it — no attendance button on the agenda or session page, and the attendance URL shows a notice — using the browser's date (D-047), with the database as the authority.                                                                                         | The owner asked for it: nobody can have attended a session that has not happened. Judged on the date, not the start time, so a coach can mark while the session is in progress or as soon as the day begins.                                                  |
| D-063 | **Expenses cannot be dated in the future**, `created_by` is always the caller, and a salary's coach must be a coach (an inactive one can still be paid for a past month). **Editing and deleting an expense is logged** (`expense.updated` / `expense.deleted`, with a snapshot).                                                                                                                                                                                                                                               | Keeps the reports honest (an expense that has not happened is not spent), and mirrors the payments rule. Expenses are hard-deleted (RLS matrix), so money that changes or disappears needs a trace.                                                           |
| D-064 | **Report figures come from three admin-checked `SECURITY DEFINER` functions** (`report_summary`, `revenue_by_month`, `expenses_by_category`): ranges inclusive on `paid_at` / `expense_date`; payments of cancelled subscriptions and removed players count; margin in basis points rounded half away from zero, `null` (shown "—") when nothing was collected; `revenue_by_month` always 12 zero-filled rows. `src/lib/reports.ts` mirrors the rounding and is tested against the same numbers.                                | Coaches can read some payments through RLS, so a plain invoker sum would leak the academy's takings. "Collected" is money received, whatever happened to the subscription; a margin over nothing collected is meaningless rather than −100 %.                 |
| D-065 | **`generate_monthly_salaries(month)` is idempotent per coach per month:** it skips a coach who has _any_ salary expense dated in that month (even one typed by hand), pays only active coaches with a salary above zero, dates the expense the 1st, copies the salary as it is at the time, refuses a month that has not begun, takes an advisory lock per month, and logs **one** `expense.salaries_generated` entry (only when something was created). The expenses page shows a preview and then "created / skipped" counts. | Running it twice (or after paying someone by hand) must never double-pay; two admins clicking together must not race; a summary entry keeps the activity feed readable.                                                                                       |
| D-066 | **CSV export is two files (payments, expenses) for the selected period, built in the browser:** all rows (read 1000 at a time), ISO dates, plain 3-decimal BD amounts, headers and labels in the UI language, UTF-8 **with BOM**, and text beginning `=`, `+`, `-`, `@` prefixed with an apostrophe.                                                                                                                                                                                                                            | One file mixing two kinds of rows is awkward in a spreadsheet; the BOM is what makes Excel read Arabic correctly; a note or name typed by a person must not become a formula. No server function is needed — RLS already limits these reads to admins.        |
| D-067 | **The reports page is lazy-loaded** (the charting library is ~380 kB, its own chunk) and the router shows a `RouteLoading` spinner when it is opened directly. The chart's **time axis stays left-to-right in Arabic** (the design-system default); the legend, tooltip and table are RTL. Series differ by shape as well as colour (bars, bars, line), and the month table below the chart carries the same numbers as text.                                                                                                   | Keeps the main bundle from growing for a screen most staff rarely open; a right-to-left time axis would put December before January under a left-anchored value axis.                                                                                         |
| D-068 | **`Money` shows a negative amount's number in a left-to-right island** (`-20.000` then the unit); positive amounts render as before. Report tables show plain signed numbers with the unit in the header.                                                                                                                                                                                                                                                                                                                       | Inside an Arabic run a leading minus sign lands after the digits (`20.000-`), which reads as a different number.                                                                                                                                              |

## Open questions for the product owner

| ID    | Question                                                                                          | Current placeholder                           | Blocks  |
| ----- | ------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------- |
| Q-001 | What is the **T-shirt fee** amount?                                                               | 5.000 BD (in use; edit in Settings)           | —       |
| Q-002 | What is the **transportation fee** amount, and is it per player or per subscription?              | 10.000 BD **per player** (in use)             | —       |
| Q-003 | Can a **coach** create subscriptions and record payments, or should that be admin-only?           | Coaches can, for their own players (in use)   | —       |
| Q-004 | Should discounts apply to the T-shirt/transport fees, or only the plan price?                     | Whole subtotal (D-013, in use)                | —       |
| Q-005 | Are there two branches (Al-Rifa' and Hamad City) that need separate reporting?                    | Single academy, no branch field               | Phase 6 |
| Q-006 | Higher-resolution / transparent logo file (SVG/PNG) and any official brand colors/fonts.          | Using the 320px JPG                           | Phase 1 |
| Q-007 | Where will it be hosted (Vercel/Netlify + hosted Supabase project)?                               | Deploy guide written generically in Phase 8   | Phase 8 |
| Q-008 | Should production use a **separate Supabase project** from the current dev one?                   | Same project during development               | Phase 8 |
| Q-009 | Should **coaches** be allowed to give a manual discount, or only admins (codes only for coaches)? | Allowed, with a reason, always logged (D-050) | Phase 8 |

_When a question is answered, move it into the tables above as a decision and update the affected docs/tests._
